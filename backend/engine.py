"""
NPFJ 引擎：二分树状态机路由 + 五维隐性权重累加器

架构：
========================================
用户会话状态 (session)：
{
    "path": [],               ← 路由路径，如 ["L", "R", "L"]
    "pool_history": [0, 2],   ← 走过的题池 ID
    "current_pool": 3,        ← 当前题池 ID
    "weights": [0,0,0,0,0],   ← 五维权重累加
    "stage_answers": {}       ← 当前题池的答案缓存
}
========================================

路由规则：
- 每个题池 5 题
- 每题两个选项（A/B），各有 route_label（L/R）和 weights[5]
- 5 题答完后：
  ① 统计 L/R 出现次数，多数决确定走向
  ② 累加 5 题的 weights 到 session.weights
  ③ 根据 route_map 路由到下一题池
- Stage 4 完成后，根据路径组合出 4 位字母人格
"""

import json
import os
from typing import Optional

ROOT = os.path.dirname(os.path.abspath(__file__))
POOLS_PATH = os.path.join(ROOT, "pools.json")


# ============================================================
#  16 种人格类型描述（纯心理学中性描述，无贬义）
#  按路径组合 [N/S][P/L][F/R][J/W] 排列
# ============================================================

# ============================================================
#  16 种人格分析报告（约 300-400 字）
#  放在结果页人格类型和五维图下方
# ============================================================
ANALYSIS_REPORTS = {
    "I_P_A_D": (
        "【能量表达 · 外放型】你在网络世界中的能量是向外辐射的。你不仅喜欢表达，更享受 把一件事说清楚、说透彻的过程。对你而言，讨论本身不是争输赢，而是逼近真相的途径。 你享受那种逐一拆解对方论据、找到核心矛盾、然后用清晰的逻辑把它摊开来的过程。 你的参与方式是主动介入的——你不会满足于只看别人怎么说，你会亲自下场，把自己的观点 摆到台面上来。在认知方式上，你依赖逻辑和分析。你相信事实和推理胜过感觉和直觉。 一条论据如果站不住脚，你会追问到底。你的表达倾向于结构化和深度化——给你一个话题， 你往往能写出一篇有条有理的分析，从背景到论据到结论，层层递进。 这种组合让你成为了评论区的「长文战神」。你不怕长篇大论，甚至乐在其中。你的说服力 不在于气势或嗓门，而在于你总能拿出数据和逻辑。但需要注意的是，不是所有的沟通场景 都适合长篇论证——有时候对方需要的不是被说服，而是被理解。如果你能学会在不同场景中 切换表达方式，你的影响力会更大。你的优势是思辨能力和表达勇气。你的盲区是有时显得 好为人师，让人觉得你在「上课」。试着在论证之前先问一句「你怎么看」，你会发现沟通 效果会好很多。试着在论证之前先问一句「你怎么看」——你会发现沟通效果会好很多。你的逻辑很强，但如果能先倾听再反驳，你的说服力会无人能挡。"
    ),
    "I_P_A_F": (
        "【能量表达 · 外放型】你有表达的热情，但不一定每次都选择长篇大论。 你更倾向于用短促精准的语言来传达核心观点——一句话说到点子上，比写一千字更让你有 成就感。你积极参与讨论，但不是以「占领评论区」的方式，而是以「关键一击」的方式。 你的认知方式是分析性的——你在脑海中快速完成了逻辑推演，然后把结论抛出来。你不会 纠缠于冗长的辩论，因为你已经想清楚了——你觉得把观点说清楚了就够了，对方能不能理解 是对方的事。你的表达是碎片化的，但这种碎片化不是浅薄——你的每一条短评都经过了压缩， 信息密度很高。你是那种「一句话让人无法反驳」的人。你的短评经常犀利到让人不知道怎么接。 你和别人讨论时，可能三五句话就已经把这个话题说透了——然后你就不想再继续了。这让 有些人觉得你有点「傲」，但熟悉你的人知道你这是效率至上的表现。你的风格在某些圈子里 很有影响力——你的那句评论可能会被反复截图引用。你的优势是语言精准和思维敏捷。 你的盲区是短评容易被断章取义，缺少上下文铺垫的发言也容易让人觉得突兀。如果能在 关键的时候多说一两句背景，别人会更信服你。如果能在关键的时候多说一两句背景，别人会更信服你。你的精准是武器，但适当的铺垫是让这把武器被看见的灯光。多说一句，效果翻倍。"
    ),
    "I_P_C_D": (
        "【能量表达 · 外放型】你在网络上的存在感很强，但你并不是靠数据和分析 来获得影响力的。你靠的是对氛围的感知和对叙事的掌控——你能把一个散乱的讨论重新框定， 用故事把大家拉到同一个频道上来。你主动参与讨论，但你的武器不是逻辑而是情境理解。 你能敏锐地感知到每个人的情绪和立场，然后找到那个大家都能接受的表达方式。你的表达 是结构化的——你有能力把一个复杂的感觉组织成一篇有条理的叙事。你不是在和稀泥， 而是在重新定义问题本身。你是讨论中的「控场者」。当大家都在争吵的时候，你往往能说出 一句让所有人安静下来的话。你的这种能力在团队协作中极为宝贵——你是那个能把散落的拼图 拼成一幅画的人。你对话题走向的敏感度很高：你知道什么时候该深入、什么时候该转移、 什么时候该幽默一下。你像一个对话中的「导演」，而不是演员。你的优势是共情能力和叙事 技巧。你的盲区是过度追求和谐可能让你回避必要的冲突——有时候必须说一些不好听的话 才能推动事情前进，而你的性格让你倾向于绕开这类对话。试着在必要时说出那个不好听但正确的话——你会发现你的控场能力不会因此消失，反而会因为你的勇气而更强。真正好的控场不是回避冲突，而是直面它。"
    ),
    "I_P_C_F": (
        "【能量表达 · 外放型】你上网的核心动力是「和大家一起开心」。你有表达欲， 但你的表达不是为了论证什么，而是为了制造氛围。你积极参与各种互动，但你的参与是轻松、 即兴、不带负担的。你依靠对语境和情绪的敏锐感知来决定说什么——你的表情包库存丰富， 你的接梗速度让人佩服。你的表达是碎片化的——你不需要把事情说完整，你只需要让气氛 热起来。你是群聊里的「气氛发动机」。有你在线，冷场是不存在的。你可能是群里表情包 最多的人，你是最早接梗也最擅长造梗的人。你的幽默感让和你聊天变成一件愉快的事。 但你也有自己的困扰——你很难在严肃话题上保持长时间的专注，而且你的轻松风格有时会 被人误解为不够认真。当别人正在讨论一个严肃的问题时，你的一句玩笑可能会让人觉得你 在打岔。你的优势是感染力与幽默感。你的盲区是深度内容难以引起你的持续兴趣。你可以 试着在严肃讨论中偶尔认真一回——你会发现你的语境感知能力在严肃场合同样有用。试着在严肃讨论中偶尔认真一回，你会发现你的语境感知能力在严肃场合同样有用。把这种能力用在正确的地方，你可以既有趣又有深度。试着在严肃讨论中偶尔认真一回，你会发现自己比想象中更有深度。把轻松和认真结合起来，你将成为既有趣又有分量的人。"
    ),
    "I_S_A_D": (
        "【能量表达 · 外放型】你并不是一个沉默的人——你只是不说话的时候比较多。 但你一旦开口，通常就是值得被记住的。你倾向于观察多于参与，但你的观察不是被动的 消遣——你在看的过程中一直在分析、在建构、在形成自己的判断。你的认知方式是分析型的， 你会把看到的信息拆解成要素，理解它们之间的因果关系。当积累到一定程度后，你会选择 一次性输出——一篇完整的、结构清晰的长文。你是那种「潜水很久但一出手就是精华帖」的人。 你的输出频率很低但质量很高。发完帖子后你通常不会留在评论区互动——你觉得该说的都在 主贴里说完了。这让你看起来有点高冷，但其实你只是不需要通过反复确认来获得满足感。 你知道你会被认真对待——你的帖子会被收藏、被引用、被转发，这就够了。你的优势是深度 思考能力和高质量输出。你的盲区是长期不参与互动会让你失去很多潜在的有价值的联系—— 评论区里的那些人可能正是你需要认识的人。偶尔留个言，打破一下「高冷」的人设，没有坏处。偶尔留个言，打破「高冷」的人设没有坏处。你可能会发现那些你一直在默默关注的人，其实也一直在等你开口。跨出一步，你的世界会更大。跨出一步，你的世界会更大。那些你一直在默默阅读的人，或许也一直在期待你的声音。你不必成为话多的人，但偶尔说一次就值得被记住。"
    ),
    "I_S_A_F": (
        "【能量表达 · 外放型】你有表达的冲动和实力，但你并不频繁出手。你大多 数时间在默默阅读和观察，但当你发现一个关键的事实错误或者一个被忽视的逻辑漏洞时， 你会忍不住开口——而且只说那一句。你的认知方式分析性很强——你能快速判断一个论点是否 站得住脚。你的表达是碎片化的，但这不意味着你思考得浅——你只是把复杂的推演压缩成了 一句话。你的评论之所以有力，是因为它经过了高度浓缩——你花在删除上的时间可能比写的 时间还长。你是那种「平时不说话，一开口就让人无法反驳」的人。你的每一句评论都踩在 关键点上，不多不少。你发言后通常不会跟进讨论——你已经贡献了最关键的信息，剩下的让 别人自己去消化。这种风格让你在某些圈子里拥有意想不到的影响力——你的那句评论可能会 被反复引用，成为整栋楼里最有价值的信息。人们不一定认识你，但他们记住了你说的那句话。 你的优势是精准的判断力和精炼的表达。你的盲区是缺少铺垫的发言容易被忽略——在这个 信息过载的时代，一句没有上下文的话很容易被划过去。如果你能让你的发言稍微「显眼」一点，你的影响力会更大。一句没有上下文的金句很容易被划过去。花十秒钟加一句铺垫，你的那句妙语会被更多人记住。"
    ),
    "I_S_C_D": (
        "【能量表达 · 外放型】你的表达欲是真实的，但你不喜欢在嘈杂的环境中发声。 你倾向于在自己被真正触动的时候才会写点什么。你的认知方式是语境驱动型的——你不是通过 分析逻辑来理解世界，而是通过感受氛围和情绪。你能敏锐地捕捉到那些微妙的情感变化， 并用自己的语言把它们表达出来。你的表达是结构化的——你写下的感受不是零散的，而是 一整段有起承转合的叙述。你是一个用心写作的人。你是那种「不发则已，一发就是一篇走心 长文」的人。你写的东西往往能打动人——不是因为逻辑有多严密，而是因为真诚。你的文字 有温度——读你的人会觉得「他懂我」。你很少参与争论，因为你表达的目的不是赢，而是 被理解。如果有人认真读了你的文字并产生了共鸣，那就是你最大的满足。你在互联网上的 存在方式更像一个「深夜写作者」——在喧嚣褪去之后，你才开始动笔。你的优势是情感感知力 和真诚的表达。你的盲区是过度暴露内心有时会让自己受伤——不是所有人都会珍惜你的真诚。 学会保护自己的边界和学会打开心扉同样重要。学会保护自己的边界和学会打开心扉同样重要。你的真诚是珍贵的——不要因为受过伤就关上那扇门。找到那些值得的人，然后继续用心写作。 。"
    ),
    "I_S_C_F": (
        "【能量表达 · 外放型】你喜欢和别人产生联系，但你不一定要站在舞台中心。 你更愿意做一个「在场的人」——你参与的方式是点赞、转发、偶尔的一句评论。你不主动挑起 话题，但如果有人开启了有意思的对话，你会很乐意接下去。你的认知方式偏向情境理解—— 你能很快感受到一个话题的氛围和基调，然后用一种不破坏气氛的方式加入。你的表达是碎片 化的——一条评论、一个表情包、一个转发，就是你的全部。但这不代表你冷漠——只是你的 互动方式比较轻。你是那种「不冒泡但也不缺席」的人。每个群都有你这样的人——存在感不高， 但如果哪天你真的不在了，大家会感觉到少了点什么。你很少成为话题的中心，但你是让话题 持续下去的重要润滑剂。你在熟人圈子里可能是那种「所有人都跟你聊得来」的人——因为你 永远不会抢风头，也不会冷场。你的优势是社交的轻松感和亲和力。你的盲区是存在感偏低， 容易被忽视。你不一定要成为焦点，但偶尔站出来表达一下自己的真实想法，会让别人更了解 你——也会让你自己更了解自己。偶尔站出来表达一下自己的真实想法，会让别人更了解你——也会让你自己更了解自己。试着多说一句，你会发现自己比想象中更有力量。沉默舒服，但开口有惊喜。"
    ),
    "R_P_A_D": (
        "【能量表达 · 内收型】你不会轻易把能量释放出去，但当你在意一件事时， 你会投入比大多数人都多的精力。你不喜欢无意义的社交和讨论——你觉得很多事情不值得 浪费口舌。但如果有人提出了一个值得认真对待的问题，你会给出深思熟虑的回答。你选择性 地参与——你只在你有话可说、有话想说的时候才开口。你的认知方式是分析性的，你做判断的 依据是逻辑而非情感。当有人用情绪化的语言攻击你时，你通常会冷静地看完，然后——要么 理性回应，要么根本不回应。你的表达是结构化的——当你要说一件事的时候，你会把它组织得 条理清晰。你是那种「不说话则已，一说就条理分明」的人。情绪化的言论无法带偏你—— 你只会安静地看完，然后给出理性的分析。你的朋友们知道当需要冷静的建议时找你准没错。 你在工作中是那种能给团队带来「定心丸」的人——当大家都慌了的时候，你是那个还在理性 思考的人。你不容易被说服，但如果你被说服了——那一定是因为对方拿出了更好的逻辑。 你的优势是理性判断力和系统表达能力。你的盲区是被人认为冷血或没有人情味——有时候 别人来找你需要的不是分析，是情感支持。学会在给出结论之前先给出共情，会让你的人际 关系更融洽。学会在给出结论之前先给出共情，会让你的人际关系更融洽。你的理智是天赋，但如果能让对方先感受到你的理解，你的分析和建议会被更好地接受。"
    ),
    "R_P_A_F": (
        "【能量表达 · 内收型】你不是一个话多的人，但你有明确的想法，并且在必要的 时候会表达出来。你参与讨论的方式是「点到即止」——说出你的观点，不需要长篇大论， 也不需要说服每一个人。你的认知方式是分析性的——你快速判断对错，然后简洁地表达你的 判断。你的表达是碎片化的，但每个碎片都有自己的分量。你不会浪费口舌在无意义的争论上 ——如果对方明显不在讲道理，你会选择直接退出对话。你不是认输，你只是不想浪费时间。 你是那种「理性但不啰嗦」的人。你很少参与无意义的争论——如果对方明显不在讲道理， 你不会浪费时间。你给出的评论往往短而有力，让人很难反驳。你不追求让所有人都认同你 ——你说完了，别人听不听是他们的事。在嘈杂的网络环境中，你是那种「安静但坚定」的 存在。你的优势是逻辑清晰和表达高效。你的盲区是缺少情感投入可能让人觉得冷漠——有时候 多说一句「我理解你的感受」并不会削弱你的观点的力量，反而会让对方更愿意听你说下去。多说一句「我理解你的感受」并不会削弱你观点的力量，反而会让对方更愿意听你说下去。在理性之前先给一点温度，你的逻辑会更有穿透力。在理性之前先给一点温度，你的逻辑会更有穿透力。你不需要变成另一个人，只需要在说完结论后多问一句「你觉得呢」。"
    ),
    "R_P_C_D": (
        "【能量表达 · 内收型】你有条理、有耐心、善于建构。你不是最活跃的讨论者， 但你是把一切组织起来的人。你参与的方式不是说很多话，而是把事情理清楚。你喜欢把零散 的信息整理成结构化的知识——你做笔记、画框架、写总结。你的认知方式是语境理解型的—— 你善于从整体上把握一件事，而不是仅仅分析它的局部。你的表达是深度和系统的——你给出的 往往是一个完整的框架，而不是一个孤立的观点。你有一种天生的「整理癖」——看到混乱的 东西你就想把它理顺。你是项目合作中那种「没有你就散架了」的人。当团队成员各说各话时， 你会主动把大家的想法整合起来，形成一个清晰的工作框架。你不一定要当领导者，但你是 一个有效的组织者。你在学习新东西时习惯于先搭一个框架再填充细节——你无法忍受在混乱 中前行。你的知识体系通常比别人更牢固，因为你从一开始就把它建在了稳固的基础上。 你的优势是整合能力和结构化思维。你的盲区是追求体系化有时会过度——不是所有的事情 都需要先搭框架才能做。学会在「不完美」的状态下推进，有时候比等一切都理顺了再动手 更有效。学会在「不完美」的状态下推进，有时候比等一切都理顺了再动手更有效。不是所有事情都需要先搭框架，有些框架是在做的过程中自然成型的。"
    ),
    "R_P_C_F": (
        "【能量表达 · 内收型】你话不多，但你不是不关心——你只是不主动。如果有人 找到你、问到你了，你通常愿意帮忙。你不会主动发起话题，但也不会拒绝有意义的互动。 你的认知方式偏向理解和感受——你能很好地理解对方的处境和需求，然后在需要的时候给出 回应。你的表达是碎片化和轻量的——你不会写长篇大论，但你会贴一个有用的链接或者一句 关键的建议。你的帮助通常是实用导向的——不给建议、直接给解决方案。你是那种「在你需要 的时候会出现」的人。你平时存在感不高，但朋友们知道你是靠谱的。你在群聊里话不多， 但如果有人问了一个你擅长领域的问题，你会给出有用的回答。你不会主动关心别人，但当 别人来找你时你也不会拒绝。你的被动并不是冷漠——只是你启动社交需要消耗能量。你可能 只有有限的几个亲密圈子，但在这几个圈子里你是被信任的。你的优势是实用主义的帮助意愿。 你的盲区是被动性格可能让你错过很多社交机会和潜在的关系——很多值得认识的人可能就因为 你不主动而永远没法认识你。偶尔主动一次，哪怕只是说一句「最近怎么样」。偶尔主动一次，哪怕只是一句「最近怎么样」——你会发现世界比你想象中更愿意回应你。你值得被更多的人记住，别总是等别人来找你。"
    ),
    "R_S_A_D": (
        "【能量表达 · 内收型】你完全是一个「能量向内收敛」的人。不上头、不冲动、 不主动表达。但你的大脑从未停止工作。你是纯粹的观察者和分析者——你看大量的内容、 读深度的文章、在心里推演各种可能性。你几乎不留下任何数字痕迹，但你的知识储备可能 远超大多数活跃用户。你的认知方式是分析性的——你在头脑中构建了完整的逻辑链条。 你对事物的理解往往比那些侃侃而谈的人更深——因为你花了更多时间在思考而不是在表达上。 你是那种「比谁都懂但从不发言」的人。你的朋友们可能在现实中才发现你对某某话题的了解 程度远超他们的预期。你享受的是理解本身——弄懂一件事带给你的满足感，远大于说出来 获得别人认可带来的满足感。你的知识结构可能是你身边人中最扎实的——因为你没有为了 「显得懂」而急于表达，你只是在默默地、持续地吸收和消化信息。你的优势是深厚的知识 储备和独立思考能力。你的盲区是知识无法外溢限制了你的影响力——你有很好的想法，但如果 永远不说出来，这些想法就只有你自己知道。试着偶尔输出一次，你会发现得到的反馈会让你 惊喜。试着偶尔输出一次，你会发现得到的反馈会让你惊喜。你有很好的想法，但永远不说出来，这些想法就只属于你一个人。分享出去，它们会产生你意想不到的价值。"
    ),
    "R_S_A_F": (
        "【能量表达 · 内收型】你安静、观察、偶尔闪现。你穿梭在各种信息流中， 从技术到娱乐到社会新闻，什么都有你的足迹。你很少留下评论——大多数时候你看了就过了。 但偶尔有那么一两条内容触动了你，你会留下一两句话，然后继续消失。你的认知方式是分析 性的——你有自己的判断标准，只不过你不需要说出来。你的表达是碎片化的——你偶尔飘过的 那一句话往往很有意思。你是一个「兴趣驱动的漫游者」——你可能今天在研究量子计算，明天 在看美食视频。你是那种「什么都看一点、什么都能聊一句」的人。你可能同时关注三四个 完全不同的兴趣领域，在每个领域都有一些浅层但真实的了解。你的知识面很广，这使得你 成了一个有趣的聊天对象——你能跟不同领域的人聊上几句。但在某些领域的深度上，你不如 那些专注的潜水者——你的知识结构是「广而浅」而不是「专而深」。这本身没有好坏，只是 不同的认知策略。你的优势是兴趣广泛和跨领域联想能力。你的盲区是碎片化的知识难以形成 体系——如果能在一个你特别喜欢的领域上稍微深入一些，你的知识会更有力量。如果能在一个你特别喜欢的领域上稍微深入一些，你的广博会变得更有力量。广度让你有趣，深度让你不可替代。找一个方向，扎下去看看。"
    ),
    "R_S_C_D": (
        "【能量表达 · 内收型】你是那个「什么都知道但从不说」的人。你有一种罕见的 洞察力——你能从零零星星的信息中拼出一个完整的画面。你看出谁在带节奏、谁在真情实感、 谁是来捣乱的。你全部看懂了，但你不说。你的认知方式是语境理解型的——你理解的不是 信息本身，而是信息背后的动机和情绪。你的共情能力很强——你能感受到发言者的情绪， 即使他们没有直接表达出来。你的表达接近零，但你的内心世界非常活跃。你是那种「最好的 倾听者」——虽然是在网上。你理解一切但不评判，你洞察一切但不介入。有时候人们会忽略 你的存在，直到某天你偶然说了一句话，别人才发现「原来你都知道啊」。你对人性的理解 可能比那些每天在网上发表长篇大论的人要深刻得多——因为你花了更多时间在「看」而不是 在「说」上。你的沉默不是空洞，而是承载着大量信息。你的优势是极强的洞察力和共情能力。 你的盲区是长期不表达让他人无法了解你的价值——如果你能偶尔把你看懂的东西说出来， 你会惊讶地发现有多少人是真的想听。偶尔把你看懂的说出来——你会惊讶地发现有多少人是真的想听。你的洞察力是这个时代稀缺的礼物，别让它只属于你自己。说出来，世界会因你更清醒。 。"
    ),
    "R_S_C_F": (
        "【能量表达 · 内收型】你在网络世界中几乎是不存在的——你不发言、不点赞、 不评论、不转发。你的参与方式是最轻量的：打开、看看、关闭。你的认知方式是被动接收的 ——你看到什么就是什么，很少主动去深挖或分析。你的表达几乎为零——你上网纯粹是为了 放松和消遣。你不上头、不站队、不参与任何争论。你享受的是网络作为「背景音」的存在 ——有信息在身边流动，但不需要你做出任何回应。你是真正的「数字自由人」。 你是互联网上的「隐形人」。平台可能记录了你的每一次访问，但在社交层面上，你几乎没有 留下任何痕迹。这种高度松弛的上网方式让你的精神负担很小——你不必在意任何人的看法， 也不必为任何争论耗费精力。但同时，这种完全被动的使用方式也意味着你放弃了网络最独特 的东西——连接和创造的可能性。你从未体验过在某个讨论中被人认同的感觉，也从未体验过 创作出受人喜欢的作品的感觉。你的优势是高度的心理松弛感。你的盲区是可能错过了太多 ——不是为了迎合别人，而是为了发现自己。如果你偶尔在网上留下一句你自己的话——不需要 是长篇大论，哪怕只是一句评论——你可能会发现另一片风景。偶尔在网上留下一句你自己的话——不需要长篇大论，哪怕只是一句评论。你可能会发现另一片风景。迈出第一步，你会发现表达没有你想象的那么可怕。"
    ),
}

PERSONALITY_MAP = {
    # I 系（外放表达）
    "I_P_A_D": ("IPAD", "逻辑爆破者",
        "评论区的长文战神。看到逻辑漏洞会忍不住拆解，享受思辨本身带来的快感。不是为了赢，是为了「说清楚」。"),
    "I_P_A_F": ("IPAF", "精准评论者",
        "能用一句话说完的绝不写一千字。短评犀利幽默，信息密度极高。精准、干脆、不啰嗦。"),
    "I_P_C_D": ("IPCD", "语境控场者",
        "带节奏的人——不是贬义。擅长把散乱的讨论重新框定，用叙事把大家拉到一个频道上来。"),
    "I_P_C_F": ("IPCF", "气氛制造者",
        "评论区发动机。表情包库存丰富，接梗天赋点满。上网就是为了大家一起开心。"),
    "I_S_A_D": ("ISAD", "结构冲击者",
        "长期潜水，但一发帖就是精华。大量输入后爆发式输出一次，然后消失。不需要解释第二遍。"),
    "I_S_A_F": ("ISAF", "精准狙击手",
        "平时不说话，一开口就让人无法反驳。只说关键的那一句，剩下的你自己消化。"),
    "I_S_C_D": ("ISCD", "情绪表达者",
        "安静但有深度。他的表达不是辩论，是倾诉。写长篇感受不是为了说服谁，是为了记录自己被打动的瞬间。"),
    "I_S_C_F": ("ISCF", "接梗王",
        "不主动挑事，但谁挑事他都接得住。点赞收藏转发表情包，轻量互动让气氛热起来。"),
    # R 系（内收克制）
    "R_P_A_D": ("RPAD", "冷静拆解者",
        "标准理性分析用户。不轻易发言，但一旦发言就准备充分。情绪化的言论无法带偏他——他只在乎逻辑。"),
    "R_P_A_F": ("RPAF", "轻量拆解者",
        "理性但不啰嗦。偶尔参与讨论，每句话踩在点上。不服来辩？没必要，我说了我的观点就够了。"),
    "R_P_C_D": ("RPCD", "结构建构者",
        "搭框架的人。主动整理信息、搭建体系、写入门指南。不在乎谁对谁错，只在乎有没有被组织好。"),
    "R_P_C_F": ("RPCF", "轻互动者",
        "有人@他就回一句。没人找他就默默刷。愿意帮忙但不愿寒暄。存在感不高但确实在。"),
    "R_S_A_D": ("RSAD", "深潜分析者",
        "长期潜水但比谁都懂。每天花几小时阅读吸收，但不发帖不评论。知识本身即奖励。"),
    "R_S_A_F": ("RSAF", "碎片思考者",
        "偶尔冒泡，但冒泡的那一句往往有意思。在多个领域浅层涉猎，什么都看一点，什么都能聊一句。"),
    "R_S_C_D": ("RSCD", "语境观察者",
        "他什么都知道，但从不说。对人和事有极强的洞察力，理解一切但不参与。享受的是理解本身。"),
    "R_S_C_F": ("RSCF", "轻观察者",
        "纯浏览型用户。互联网的隐形人。不点赞不评论不转发，唯一存在的证据就是DAU里的一个数字。"),
}

# 四个维度的字母映射（路径方向 → 人格字母）
# Stage 1: L=I, R=R  （能量表达：外放/内收）
# Stage 2: L=P, R=S  （参与方式：主动/观察）
# Stage 3: L=A, R=C  （认知方式：分析/语境）
# Stage 4: L=D, R=F  （表达方式：深度/碎片）
DIM_LETTERS = [
    {"L": "I", "R": "R"},  # Stage 1
    {"L": "P", "R": "S"},  # Stage 2
    {"L": "A", "R": "C"},  # Stage 3
    {"L": "D", "R": "F"},  # Stage 4
]


class NPFJEngine:
    """NPFJ 二分树路由引擎"""

    def __init__(self):
        self.pools = self._load_pools()

    def _load_pools(self) -> dict:
        """加载题库 JSON"""
        if not os.path.exists(POOLS_PATH):
            return {}
        with open(POOLS_PATH, "r", encoding="utf-8") as f:
            data = json.load(f)
        return data.get("pools", {})

    def get_meta(self) -> dict:
        """返回题库元信息"""
        if not os.path.exists(POOLS_PATH):
            return {}
        with open(POOLS_PATH, "r", encoding="utf-8") as f:
            data = json.load(f)
        return data.get("meta", {})

    def create_session(self) -> dict:
        """创建新会话（用户开始测试时调用）"""
        return {
            "path": [],               # 路由路径，如 ["L", "R", "L"]
            "pool_history": [1],      # 走过的题池，从 1（能量开关）开始
            "current_pool": 1,        # 当前题池 ID
            "weights": [0, 0, 0, 0, 0],  # 五维权重累加
            "done": False,            # 是否已完成
        }

    def get_pool_questions(self, pool_id: int) -> Optional[list]:
        """
        获取指定题池的题目（给前端用，过滤掉权重等内部数据）
        返回每题的 id、text、选项文字（不含 route_label 和 weights）
        """
        pool = self.pools.get(str(pool_id))
        if not pool:
            return None

        safe_questions = []
        for q in pool.get("questions", []):
            safe_q = {
                "id": q["id"],
                "text": q["text"],
                "options": [
                    {"key": "A", "text": q["options"]["A"]["text"]},
                    {"key": "B", "text": q["options"]["B"]["text"]},
                    {"key": "C", "text": q["options"]["C"]["text"]},
                    {"key": "D", "text": q["options"]["D"]["text"]},
                ]
            }
            safe_questions.append(safe_q)
        return safe_questions

    def get_pool_info(self, pool_id: int) -> Optional[dict]:
        """返回题池信息（不含题目细节）"""
        pool = self.pools.get(str(pool_id))
        if not pool:
            return None
        return {
            "id": pool_id,
            "stage": pool["stage"],
            "name": pool["name"],
            "description": pool.get("description", ""),
        }

    def submit_pool_answers(self, session: dict, pool_id: int, answers: list) -> Optional[dict]:
        """
        提交当前题池的 5 个答案，处理路由和权重累加

        参数：
            session: 当前用户会话
            pool_id: 当前题池 ID
            answers: ["A", "B", "A", "A", "B"] 长度 5 的列表

        返回：
            {
                "next_pool": int | None,    # 下一题池 ID，None 表示已完成
                "stage": int,               # 当前阶段
                "is_final": bool,           # 是否已完成所有阶段
                "route_result": str,        # 本阶段路由结果（L/R）
            }
        """
        pool = self.pools.get(str(pool_id))
        if not pool:
            return None

        questions = pool.get("questions", [])
        if len(answers) != len(questions):
            return None

        # 统计路由方向（引擎只负责路由，权重由 calculator.py 处理）
        route_counts = {"L": 0, "R": 0}
        for i, ans in enumerate(answers):
            q_data = questions[i]
            opt = q_data["options"].get(ans.upper(), q_data["options"].get("A"))
            label = opt["route"]
            route_counts[label] = route_counts.get(label, 0) + 1

        # 路由：多数决
        route_result = "L" if route_counts.get("L", 0) >= route_counts.get("R", 0) else "R"
        session["path"].append(route_result)

        # 根据 route_map 确定下一题池
        route_map = pool.get("route_map", {})
        next_pool_str = route_map.get(route_result)

        next_pool = None
        is_final = False

        if next_pool_str is None:
            # Stage 0（基准校准）：固定路由到 Stage 1（pool 1）
            next_pool = 1
        elif isinstance(next_pool_str, int):
            next_pool = next_pool_str
        elif next_pool_str in ("D", "F", "J", "W"):
            # Stage 4：最终决出，没有下一题池
            is_final = True
            next_pool = None

        session["current_pool"] = next_pool
        if next_pool is not None:
            session["pool_history"].append(next_pool)

        if is_final:
            session["done"] = True

        return {
            "next_pool": next_pool,
            "stage": pool["stage"],
            "is_final": is_final,
            "route_result": route_result,
        }

    def finalize_result(self, session: dict) -> Optional[dict]:
        """
        最终结算：生成人格类型 + 五维雷达图数据

        参数：
            session: 已完成所有阶段的会话

        返回：
            {
                "type": "NPFJ",
                "type_title": "全栈掌控者",
                "description": "...",
                "path": ["L", "R", "L", "R"],
                "dimensions": [...],
                "radar_data": [...],
            }
        """
        if not session["done"]:
            return None

        # 从路径生成 4 位字母
        path = session["path"]  # ["L", "L", "L", "L", "L"]
        # 路径正好 4 步（Stage 1-4），全部用于人格判定
        letters = []
        for i, direction in enumerate(path):
            if i < len(DIM_LETTERS):
                letters.append(DIM_LETTERS[i].get(direction, "?"))
        type_code = "".join(letters)  # 如 "NPLF"

        # 构建 PERSONALITY_MAP 的查询键："N_P_L_F"
        map_key = "_".join(letters)

        # 获取人格描述
        personality = PERSONALITY_MAP.get(map_key)

        if personality:
            _, title, desc = personality
        else:
            title = "未命名人格"
            desc = "描述待补充"

        # 分析报告
        report = ANALYSIS_REPORTS.get(map_key, "暂无分析报告")

        # 生成维度信息
        dim_names = self.get_meta().get("dimensions", ["D1", "D2", "D3", "D4"])
        dim_labels = self.get_meta().get("dimension_names", ["维度一", "维度二", "维度三", "维度四"])
        dimensions = []
        for i in range(min(4, len(path))):
            direction = path[i]
            letter = DIM_LETTERS[i].get(direction, "?")
            opposite = "R" if direction == "L" else "L"
            opp_letter = DIM_LETTERS[i].get(opposite, "?")

            # 为每个方向给一个分布分数（基于路径确定性）
            # 这里简化：L 方向给 7-9 分，R 方向给 3-5 分
            base = 8 if direction == "L" else 4
            dimensions.append({
                "name": dim_labels[i] if i < len(dim_labels) else dim_names[i],
                "abbr": letter,
                "score": base,
                "opposite": opp_letter,
            })

        # 路径字母表示
        stage_names = ["能量表达", "参与方式", "认知方式", "表达方式"]
        path_letters = []
        for i, direction in enumerate(path):
            letter = DIM_LETTERS[i].get(direction, "?") if i < len(DIM_LETTERS) else "?"
            stage_name = stage_names[i] if i < len(stage_names) else f"阶段{i+1}"
            path_letters.append({
                "stage": stage_name,
                "direction": direction,
                "letter": letter,
            })

        return {
            "type": type_code,
            "title": title,
            "description": desc,
            "path": path,
            "path_letters": path_letters,
            "dimensions": dimensions,
            "report": report,
        }
